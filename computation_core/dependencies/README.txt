Download noisemodelling source then go to wps_script folder
Install and use gradle to generate wps jar file:

gradle jar

Then generate all dependencies jar files with the following command

gradle copyDependencies

Copy the file build/libs/wps_scripts.jar into dependencies folder

Copy all files of the dependencies folder here (next to the README file)

